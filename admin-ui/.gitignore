# Next.js
.next/
out/
build/
node_modules/

# TS build info
*.tsbuildinfo

# Test output
coverage/

# Playwright E2E — reports, failure artefacts, and saved auth sessions.
# storageState (e2e/.auth/) holds live Keycloak tokens — never commit it.
playwright-report/
test-results/
e2e/.auth/

# Local env
.env
.env*.local

# OS
.DS_Store
