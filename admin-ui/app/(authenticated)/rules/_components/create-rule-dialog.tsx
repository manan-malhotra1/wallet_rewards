/**
 * Legacy alias — see /campaigns for the actual dialog component.
 */
export { CreateCampaignDialog as CreateRuleDialog } from "@/app/(authenticated)/campaigns/_components/create-campaign-dialog";
