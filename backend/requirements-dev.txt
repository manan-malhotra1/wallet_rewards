-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
# Pinned exactly: the repo is formatted with this release, and `ruff format
# --check` in `make check` fails on formatter drift between ruff versions.
# Bumping it = reformat the repo in the same commit.
ruff==0.16.1
mypy>=1.13.0
freezegun>=1.5.0
respx>=0.22.0
